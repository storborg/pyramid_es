language: python

python:
  - "2.7"
  - "3.3"
  - "3.4"

services:
  - elasticsearch

install:
  - "pip install webtest coverage nose-cover3 flake8 --use-mirrors"
  - "pip install -e . --use-mirrors"

before_script: "flake8"

script:  "nosetests"

after_success:
  - coveralls
